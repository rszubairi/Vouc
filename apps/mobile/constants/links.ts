// Public web app URL, used as the non-member share fallback (the /app
// download landing page) and as the base for /share/... links.
export const WEB_APP_URL = "https://vouch-three-navy.vercel.app";
